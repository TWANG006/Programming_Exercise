#pragma once

#include "common.h"

class UVA_11364
{
public:

	void Solution()
	{

	}

	void Solution(const char* input, const char* output)
	{
		FILE *cfin = fopen(input, "r");
		FILE *cfout = fopen(output, "w");



		fclose(cfin);
		fclose(cfout);
	}
};